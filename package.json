{
  "name": "workspace",
  "npmClient": "yarn",
  "private": true,
  "workspaces": [
    "packages/*",
    "templates/*"
  ],
  "scripts": {
    "format": "prettier --write \"./**/*.js\"",
    "lint": "prettier --check \"./**/*.js\""
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
